bool searchMatrix(int** arr, int s, int* s2, int t) {
    int flag = 0;
  for(int x = 0 ; x <s ; x++)
  {
    for(int i = 0 ; i < *s2 ; i++)
    {
        if(arr[x][i] == t)
        {
            flag = 1;
            break;
        }
    }
  }
  if(flag == 1) return true;
  

   return false;
}
